# このプロジェクトの Python コードは標準ライブラリのみで動きます（追加の pip 依存なし）。
#
# 別途、以下がシステムに必要です:
#   - ffmpeg                 動画の書き出し（apt-get install ffmpeg / brew install ffmpeg）
#   - Node.js + playwright   HTML->PNG レンダリング（npm i -g playwright）
#                            ブラウザ本体は `npx playwright install chromium`（本開発環境は導入済み）
